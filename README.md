# Log Archive CLI Tool

A simple Bash CLI tool that compresses log files from a specified directory into a tar.gz archive and logs the archive date/time.

## Usage

```bash
./log-archive.sh /var/log
#project url https://github.com/Arch-Official02/log-archive-cli.git
